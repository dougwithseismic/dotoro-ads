# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/dotoro

# Server
PORT=3001

# CORS Origins (comma-separated)
# Includes web app, API, and marketing/docs site
# For production: https://app.dotoro.io,https://api.dotoro.io,https://dotoro.io
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:4000

# Frontend URL (for OAuth redirects)
FRONTEND_URL=http://localhost:3000

# Reddit Ads OAuth
# Create app at: https://www.reddit.com/prefs/apps
# Set redirect URI to: http://localhost:3001/api/v1/reddit/auth/callback
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
REDDIT_REDIRECT_URI=http://localhost:3001/api/v1/reddit/auth/callback

# Meta (Facebook) Ads OAuth
# Create app at: https://developers.facebook.com/apps/
# 1. Create a new app (Business type)
# 2. Add Facebook Login product
# 3. Add Marketing API product (requires Business verification for production)
# 4. Configure Valid OAuth Redirect URIs in Facebook Login settings
# 5. Get App ID and App Secret from App Settings > Basic
# Set redirect URI to: http://localhost:3001/api/v1/auth/meta/callback
META_APP_ID=your_meta_app_id
META_APP_SECRET=your_meta_app_secret
META_REDIRECT_URI=http://localhost:3001/api/v1/auth/meta/callback

# Google OAuth (for Google Sheets & Google Ads integration)
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Enable APIs: Google Sheets API, Google Drive API, Google Ads API
# Set redirect URI to: http://localhost:3001/api/v1/auth/google/callback
# See README for full setup instructions
GOOGLE_CLIENT_ID=123456789012-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxxxxxxxxxx
GOOGLE_REDIRECT_URI=http://localhost:3001/api/v1/auth/google/callback

# Encryption (for OAuth token storage)
# IMPORTANT: Generate a secure key for production!
# Generate key with: openssl rand -hex 32
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Better Auth Configuration
# IMPORTANT: Generate a secure secret for production!
# Generate secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-32-byte-secret-here-change-in-production
BETTER_AUTH_URL=http://localhost:3001
WEB_URL=http://localhost:3000

# OAuth Providers (Optional)
# These enable social sign-in. If not set, OAuth providers will be disabled.

# Google OAuth
# Create credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing
# 2. Configure OAuth consent screen (External, add test users for dev)
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized redirect URI: http://localhost:3001/api/auth/callback/google
AUTH_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
AUTH_GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
# Create OAuth App at: https://github.com/settings/developers
# 1. Click "New OAuth App"
# 2. Set Homepage URL: http://localhost:3000
# 3. Set Authorization callback URL: http://localhost:3001/api/auth/callback/github
AUTH_GITHUB_CLIENT_ID=your-github-client-id
AUTH_GITHUB_CLIENT_SECRET=your-github-client-secret

# Email Configuration
# Sign up at: https://resend.com (free tier: 100 emails/day)
# In development without RESEND_API_KEY, emails are logged to console instead
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com
MAGIC_LINK_BASE_URL=http://localhost:3000

# App URL (used for invitation links)
# This should match your frontend URL where users access the app
APP_URL=http://localhost:3000

# R2/S3 Compatible Storage
# For Cloudflare R2: https://{ACCOUNT_ID}.r2.cloudflarestorage.com
STORAGE_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
STORAGE_BUCKET=dotoro-assets
STORAGE_ACCESS_KEY=your-r2-access-key-id
STORAGE_SECRET_KEY=your-r2-secret-access-key
STORAGE_REGION=auto
# Optional: CDN URL for public asset delivery (if R2 bucket is public)
CDN_URL=https://assets.yourdomain.com

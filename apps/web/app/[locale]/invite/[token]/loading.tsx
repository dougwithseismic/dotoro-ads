/**
 * Invitation Page Loading State
 *
 * Shows a loading skeleton while the page is loading.
 */
import { LoadingSkeleton } from "./components/LoadingSkeleton";

export default function InvitationLoading() {
  return <LoadingSkeleton />;
}
